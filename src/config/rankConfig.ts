import type { RankConfig } from '../types/common';

const rankConfig: RankConfig = {
	student: 'Học sinh',
	teacher: 'Giáo viên',
	manager: 'Giám thị',
	admin: 'Ban giám hiệu',
	owner: 'Hiệu trưởng',
};

export default rankConfig;
